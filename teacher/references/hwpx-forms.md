# HWPX 양식 workflow 위치 변경

HWP/HWPX 기본 엔진은 `kordoc`(`references/kordoc.md`)이다 — 한컴 COM 없이 `.hwp`/`.hwpx`를 읽고 서식 보존 패치한다.
HWPX 구조 편집, 이미지/서명 삽입 등 예외 경로는 `references/workflows/hwpx-forms.md`를 따른다.

이 파일은 기존 참조 경로 호환을 위한 안내용 문서다.
